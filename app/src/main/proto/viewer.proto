syntax = "proto3";

option java_package = "com.san.kir.manger";
option java_multiple_files = true;

message Viewer {
  string orientation = 1;
  bool cutout = 2;

  message Control {
    bool taps = 1;
    bool swipes = 2;
    bool keys = 3;
  }

  Control control = 3;
}
